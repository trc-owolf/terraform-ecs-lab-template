terraform {
  /*
  backend "s3" {
    bucket = "<BUCKS3_BUCKET_ID>"
    key    = "statefiles/lab4/codecommit"
    region = "eu-central-1"

    # Required for execution in the Lab environment
    # Add value from the environment variable and remove comments

    assume_role {
      role_arn    = "<LAB_AWS_ROLE_ARN>"
    }
  }
  */
}
